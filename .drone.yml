kind: pipeline
type: kubernetes
name: publish

trigger:
  event: [push]

steps:
  -name: build
  image: plugins/docker
  settings:
    username: quay_username
    passwor: quay_password
    registry: quay.io
    repo: quay.io/tim_zurbuchen/zebbra_store
    mtu: 1316
